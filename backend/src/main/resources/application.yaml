spring:
  application:
    name: BirthCalculator
  datasource:
    url: jdbc:h2:file:./backend/data/benefit-db;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    #  jpa:
    #    hibernate:
    #      ddl-auto: update
    properties.hibernate.format_sql: true
    show-sql: true


